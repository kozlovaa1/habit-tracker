# Local overrides example (this file is safe to commit, but do NOT commit real secrets).
# Copy to .env.local and fill with your actual local credentials (Git-ignored by default).

###> symfony/framework-bundle ###
APP_ENV=dev
APP_SECRET=ChangeMe_32chars_minimum_local_secret
###< symfony/framework-bundle ###

POSTGRES_DB=app
POSTGRES_USER=app
POSTGRES_PASSWORD=ChangeMe

# Pick the DB you actually use locally
# DATABASE_URL="sqlite:///%kernel.project_dir%/var/data_%kernel.environment%.db"
# DATABASE_URL="mysql://app:ChangeMe@127.0.0.1:3306/app?serverVersion=8.0.32&charset=utf8mb4"
DATABASE_URL="postgresql://app:ChangeMe@127.0.0.1:5432/app?serverVersion=16&charset=utf8"

REDIS_URL="redis://127.0.0.1:6379"
