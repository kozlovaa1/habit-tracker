# Example environment configuration (commit-safe)
# Copy to .env.local (ignored by Git) and adjust values for your machine.

###> symfony/framework-bundle ###
APP_ENV=dev
# Generate a random 32+ char secret for local/dev; do not commit real secrets
APP_SECRET=ChangeMe_32chars_minimum_local_secret
###< symfony/framework-bundle ###

# Database settings (choose one and adjust)
# DATABASE_URL="sqlite:///%kernel.project_dir%/var/data_%kernel.environment%.db"
# DATABASE_URL="mysql://app:ChangeMe@127.0.0.1:3306/app?serverVersion=8.0.32&charset=utf8mb4"
DATABASE_URL="postgresql://app:ChangeMe@127.0.0.1:5432/app?serverVersion=16&charset=utf8"

# Optional Postgres convenience variables used by docker compose
POSTGRES_DB=app
POSTGRES_USER=app
POSTGRES_PASSWORD=ChangeMe

# Redis (if used)
REDIS_URL=redis://127.0.0.1:6379

# Mercure demo defaults (override if you enable Mercure)
CADDY_MERCURE_JWT_SECRET=ChangeThisMercureHubJWTSecretKey
